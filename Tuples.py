d = (1,2,3)

# Tuples are immutable i.e u cant change the items inside the list once entered, u cannot append or overlap some item in list.


print "\nLength: %s\n" %(len(d))

print "Index: %s\n" %(d.index(1))

print "Count: %s\n" %(d.count(2))




